#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x, d, t, q;

    scanf("%d", &x);
    d = x*2;
    t = x*3;
    q = x*4;

    printf("%d\n", d);
    printf("%d\n", t);
    printf("%d", q);

    return 0;
}
