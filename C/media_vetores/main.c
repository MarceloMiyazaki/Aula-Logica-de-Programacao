#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i;
    double v[10], v1[10], v2[10], avg;

    for(i==0;i<10;i++){
        printf("Nota do aluno %d: ", i);
        scanf("%lf", &vet[i]);
        avg += vet[i];
    }
    avg /= 10;

    return 0;
}
